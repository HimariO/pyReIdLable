from setuptools import setup

setup(name='pyReIdLable',
      version='0.0',
      install_requires=[
          'moviepy',
          'PIL',
          'progress',
          'scipy',
          'numpy'
      ],
      zip_safe=False)
